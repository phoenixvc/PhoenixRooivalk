# Allow panicking in tests
allow-unwrap-in-tests = true
allow-expect-in-tests = true

# Warn on common style issues
style = "warn"

# Warn on missing documentation
missing-docs-in-private-items = "warn"
