---
title: Product List Summary
sidebar_label: Product list
---

import {
  getProductLines,
  getProductListSummary,
  getProductsByLine,
} from "@site/src/data/products";

const summary = getProductListSummary(); const lines = getProductLines();

# Product List Summary

Quick reference for all products in the PhoenixRooivalk catalog. Source of truth
is the TypeScript catalog; see
[Product catalog data source](./product-catalog-source.mdx) for location and
export.

**Totals:** {summary.totalProducts} products across {summary.lineCount} lines.
`lastUpdated` in catalog: **{summary.lastUpdated}**.

---

{lines.map((line) => (

  <section key={line.id}>
    <h2>{line.id}{line.subtitle ? ` (${line.subtitle})` : ""} — {summary.countsByLine[line.id]}</h2>

    <table>
      <thead>
        <tr>
          <th>SKU</th>
          <th>Name</th>
          <th>Price range</th>
        </tr>
      </thead>
      <tbody>
        {getProductsByLine(line.id).map((p) => (
          <tr key={p.sku}>
            <td>{p.sku}</td>
            <td>{p.name}</td>
            <td>{p.priceRange}</td>
          </tr>
        ))}
      </tbody>
    </table>

    <hr />

  </section>
))}

## Related

- [Product catalog data source](./product-catalog-source.mdx) — Repo location,
  usage, and JSON/CSV export.
- [PRD index](../prd/index.mdx) — Product requirement documents.
