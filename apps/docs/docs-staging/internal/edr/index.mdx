# EDR — Engineering Decision Records

Engineering decision records. Use `edr-template.mdx` and date-prefixed filenames: `YYYY-MM-DD-short-title.mdx` (e.g. IMU standardization, motor driver tiers, transport abstraction baseline).
