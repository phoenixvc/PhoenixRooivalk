# Phoenix Rooivalk Documentation Configuration
# Copy this file to .env.local for local development
# For production, set these in your hosting platform (GitHub Secrets, Azure, etc.)

# =============================================================================
# Cloud Provider Selection
# =============================================================================
# Set to 'azure' to use Azure Entra ID, or 'firebase' to use Firebase
CLOUD_PROVIDER=azure

# =============================================================================
# Azure Entra ID Configuration (if CLOUD_PROVIDER=azure)
# =============================================================================
# Get these from: Azure Portal → Microsoft Entra ID → App registrations → Your app

# Required - Application (client) ID from app registration
AZURE_ENTRA_CLIENT_ID=your-client-id-here

# Required - Directory (tenant) ID
AZURE_ENTRA_TENANT_ID=your-tenant-id-here

# Required - Client secret (from Certificates & secrets)
# Note: This is only needed for server-side operations
AZURE_ENTRA_CLIENT_SECRET=your-client-secret-here

# Optional - Authority URL (defaults to https://login.microsoftonline.com/{tenant-id})
AZURE_ENTRA_AUTHORITY=https://login.microsoftonline.com/your-tenant-id-here

# Optional - Redirect URI after login (defaults to current origin)
AZURE_ENTRA_REDIRECT_URI=http://localhost:3000/callback

# Optional - Redirect URI after logout (defaults to current origin)
AZURE_ENTRA_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Optional - OAuth scopes (space-separated, defaults to "openid profile email User.Read")
AZURE_ENTRA_SCOPES=openid profile email User.Read

# =============================================================================
# Azure Services Configuration
# =============================================================================
# Azure Functions base URL (for AI/backend functions)
AZURE_FUNCTIONS_BASE_URL=https://your-function-app.azurewebsites.net

# Azure Application Insights (optional, for analytics)
AZURE_APP_INSIGHTS_CONNECTION_STRING=InstrumentationKey=xxx;IngestionEndpoint=xxx

# =============================================================================
# Firebase Configuration (if CLOUD_PROVIDER=firebase)
# =============================================================================
# Get these from: Firebase Console → Project Settings → General → Your apps → Web app

# Required - Firebase Project Settings
FIREBASE_API_KEY=your-api-key-here
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id

# Optional - Google Analytics 4 (for GA4 integration)
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# Notes
# =============================================================================
# - These environment variables are read at BUILD TIME for Docusaurus
# - Firebase/Azure client configs are exposed to the browser (this is expected)
# - Security is handled via backend rules (Firestore Rules, Azure RBAC), not by hiding keys
# - Never commit .env.local to version control
