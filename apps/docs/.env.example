# Firebase Configuration for Phoenix Rooivalk Documentation
# Copy this file to .env.local for local development
# For production, set these in your hosting platform (Netlify, Vercel, etc.)

# Required - Firebase Project Settings
# Get these from: Firebase Console → Project Settings → General → Your apps → Web app
FIREBASE_API_KEY=your-api-key-here
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id

# Optional - Google Analytics 4 (for GA4 integration)
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Netlify Setup Instructions:
# 1. Go to: Site settings → Build & deploy → Environment
# 2. Add each variable above with its value
# 3. Trigger a new deploy (the values are read at BUILD TIME, not runtime)
#
# IMPORTANT: These are NOT secret - they are exposed to the client.
# Firebase security is handled via Firestore Security Rules, not by hiding these keys.
